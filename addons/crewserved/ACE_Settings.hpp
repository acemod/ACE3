class ACE_Settings {
    class GVAR(onlyReloadEmpty) {
        typeName = "BOOL";
        value = 0;
        displayName = CSTRING(onlyReloadEmpty_Name);
        // description = CSTRING(onlyReloadEmpty_Desc);
    };
};
